<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="#FFF8E1">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@drawable/quote_gradient_background">

        <!-- 💭 Quote Icon -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="💭"
            android:textSize="24sp"
            android:layout_gravity="start"
            android:layout_marginBottom="8dp" />

        <!-- 📝 Quote Text -->
        <TextView
            android:id="@+id/textQuote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Success is not final, failure is not fatal: it is the courage to continue that counts."
            android:textColor="#2E7D2E"
            android:textSize="16sp"
            android:textStyle="italic"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="12dp"
            tools:text="Success is not final, failure is not fatal: it is the courage to continue that counts." />


        <TextView
            android:id="@+id/textAuthor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="— Winston Churchill"
            android:textColor="#1B5E20"
            android:textSize="14sp"
            android:textStyle="bold"
            android:gravity="end"
            tools:text="— Winston Churchill" />

        <!-- 🌟 Motivational Accent -->
        <View
            android:layout_width="60dp"
            android:layout_height="3dp"
            android:layout_marginTop="8dp"
            android:background="#FF9800"
            android:layout_gravity="end" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>